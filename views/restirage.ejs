<%- include("head") -%>

<table>
  <tr>
    <th>Film</th>
    <th>Proposé par</th>
    <th>Genre</th>
  </tr>
  <% results.forEach(result => { %>
  <tr>
    <td><%= result.title %></td>
    <td><%= peoples.find(p => p.person_id === result.person_id).name %></td>
    <td><%= tags.find(t => t.tag_id === result.tag_id).genre %></td>
  </tr>
  <% }) %>
</table>

<%- include("footer") -%>
